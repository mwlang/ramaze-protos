# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
dev:
  adapter: sqlite
  database: db/proto_development.db

# Warning: The database defined as "spec" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
spec:
    adapter: sqlite
    database: db/proto_test.db

live:
    adapter: sqlite
    database: db/proto_live.db

